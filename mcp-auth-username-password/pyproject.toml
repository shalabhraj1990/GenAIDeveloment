[project]
name = "mcp-auth-username-password"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "fastapi[standard]>=0.128.0",
    "fastmcp>=2.14.2",
    "passlib[bcrypt]>=1.7.4",
]
